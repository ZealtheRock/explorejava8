package com.harsh.explore.streams;import java.util.Arrays;import java.util.HashMap;import java.util.List;import java.util.Map;public class TwoSumFinder {    public static int[] findTwoSum(List<Integer> nums, int target) {        Map<Integer, Integer> map = new HashMap<>();        for (int i = 0; i < nums.size(); i++) {            int complement = target - nums.get(i);            if (map.containsKey(complement)) {                return new int[]{map.get(complement), i};            }            map.put(nums.get(i), i);        }        return new int[]{-1, -1}; // Not found    }    public static void main(String[] args) {        List<Integer> nums = Arrays.asList(1, 2, 4, 5, 7, 8);        int target = 15;        int[] result = findTwoSum(nums, target);        System.out.println(Arrays.toString(result)); // Output: [0, 1]    }}